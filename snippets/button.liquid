{% comment %}
Required values
label: <string>
______________
Optional values
href: <string>
type: <string>
style: <string>
class: <string>
attribute: <string>
{% endcomment %}

{% if label != blank %}
  {% if type == "link" %}
    <a {% if href != blank %}href="{{ href }}"{% endif %} class="button " {{ attribute }}>{{ label }}</a>
  {% else %}
    <button type="{{ type }}" class="button " {{ attribute }}>{{ label }}</button>
  {% endif %}
{% endif %}
